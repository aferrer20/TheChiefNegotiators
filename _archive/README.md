# Archive — not published

GitHub Pages skips folders that start with an underscore, so nothing in here is served at thechiefnegotiators.com.

- `app.html`, `app v1.html`, `app v2.html` — earlier homepage drafts (July 2026) from a superseded "GPU desk / live-stock" positioning. Contradict the current no-inventory model; kept for reference only.
- `deploy/` — older solo-consultant site and its "Restricted Access" gate.
- `copilot.html` — tool-generated bundle experiment, never converted to a page.
- `preview/`, `tweaks-panel.jsx`, `colors_and_type.css` — design-system previews and helpers from the site build tool.
- `uploads/` — raw source images.

To restore anything, `git mv` it back to the repository root.
